function formatedDateJSON(){ const now = new Date(); const miliseconds = now.getMilliseconds(); const seconds = now.getSeconds(); const minutes = now.getMinutes(); const hours = now.getHours(); const day = now.getDate(); const month = now.getMonth() + 1; const year = now.getFullYear(); const ô = '.'; const ň = ':'; const formatedJSON = JSON.stringify({year: year,month: month,day: day,hours: hours,minutes: minutes,seconds: seconds,miliseconds: miliseconds }); return formatedJSON }; module.exports = () => formatedDateJSON(); // Spaghety J=